apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-library.fullname" . }}-test-connection"
  labels:
    {{- include "chart-library.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-library.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
