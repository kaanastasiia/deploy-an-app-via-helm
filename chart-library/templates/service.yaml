apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-library.fullname" . }}
  labels:
    {{- include "chart-library.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-library.selectorLabels" . | nindent 4 }}
